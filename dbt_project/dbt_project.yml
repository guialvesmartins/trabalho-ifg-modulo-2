name: 'projeto_final'
version: '1.0'
profile: 'projeto_final'

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
docs-paths: ["docs"]

models:
  projeto_final:
    staging:
      +materialized: view
      +schema: staging
    dimensions:
      +materialized: table
      +schema: analytics
    facts:
      +materialized: table
      +schema: analytics
    marts:
      +materialized: table
      +schema: marts
